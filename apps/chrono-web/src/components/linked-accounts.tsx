// Foundation component — implementation lives in `agora/ui`. Re-exported here so
// existing app call sites keep importing from `@/components/linked-accounts`.
export { LinkedAccounts } from "agora/ui";
