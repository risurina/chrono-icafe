// Foundation component — implementation lives in `agora/ui`. Re-exported here so
// existing app call sites keep importing from `@/components/social-sign-in`.
export { SocialSignIn, useAuthProviders } from "agora/ui";
