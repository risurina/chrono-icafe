// Foundation component — implementation lives in `agora/ui`. Re-exported here so
// existing app call sites keep importing from `@/components/tenant-brand-header`.
export { TenantBrandHeader } from "agora/ui";
