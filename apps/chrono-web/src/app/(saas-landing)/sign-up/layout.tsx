import { StaffAuthChrome } from "@/components/staff-auth-chrome";

export default function Layout({ children }: { children: React.ReactNode }) {
  return <StaffAuthChrome>{children}</StaffAuthChrome>;
}
